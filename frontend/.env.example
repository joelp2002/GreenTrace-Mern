# Browser calls this base URL. For local dev with Vite proxy, you can use relative URLs:
# VITE_API_URL=
# Or set full backend URL (required for production build on Vercel):
VITE_API_URL=http://localhost:5001
